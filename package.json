{
  "name": "farmacia-bot",
  "version": "1.0.0",
  "description": "Bot simples para gerenciar estoque e respostas automáticas (demo CLI).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "api": "node src/server.js",
    "lint": "echo \"No lint configured\"",
  "test": "jest --coverage",
  "test:api": "jest __tests__/api.test.js --runInBand",
    "test:handlers": "node scripts/test-handlers.js",
    "test:cli": "node scripts/test-cli-integration.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^5.1.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "zod": "^3.22.2"
  }
}
